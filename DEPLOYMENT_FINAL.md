# 🎉 客户演示环境 - 部署方案总结

> 完整的部署方案，包括 Vercel 和 Render 两种选择。

---

## 📦 已创建的文件清单

### 部署配置
| 文件 | 说明 |
|-----|------|
| `render.yaml` | Render Blueprint 配置文件 |
| `render-docker.yaml` | Render Docker 部署配置 |
| `apps/api/Dockerfile` | API 容器化配置 |
| `apps/api/api/serverless-wrapper.js` | Vercel Serverless 适配 |
| `apps/api/api/health.js` | 健康检查端点 |
| `apps/api/api/test.js` | 测试端点 |

### 文档
| 文件 | 说明 |
|-----|------|
| `DEPLOYMENT_GUIDE.md` | Vercel 详细部署指南 |
| `RENDER_DEPLOYMENT.md` | Render 详细部署指南 |
| `DEPLOYMENT_SUMMARY.md` | 一页纸快速参考 |
| `DEPLOYMENT_STATUS.md` | 当前部署状态 |
| `DEPLOYMENT_FINAL.md` | 本总结文档 |

### 脚本
| 文件 | 说明 |
|-----|------|
| `scripts/deploy-render.sh` | Render 部署准备脚本 |

---

## 🚀 推荐的部署方案: Render

由于 Vercel API 服务超时问题，**强烈建议使用 Render**。

### 为什么选择 Render?
| 特性 | Vercel | Render |
|-----|--------|--------|
| Node.js 支持 | ⚠️ Serverless 有限制 | ✅ 原生支持 |
| PostgreSQL | ❌ 需外部服务 | ✅ 内置免费 |
| 长连接 | ❌ 超时限制 | ✅ 无限制 |
| 部署复杂度 | 中等 | 简单 |
| 免费稳定性 | 一般 | 良好 |

---

## 📋 Render 部署步骤 (3 步)

### Step 1: 登录
访问 https://dashboard.render.com，使用 GitHub 登录

### Step 2: 创建 Blueprint
```bash
# 或者运行脚本查看提示
./scripts/deploy-render.sh
```

在 Render Dashboard:
1. 点击 **New +** → **Blueprint**
2. 选择 `jxhhdx/smart-dispatch-rules`
3. 点击 **Apply**

### Step 3: 等待部署完成
Render 会自动：
- ✅ 创建 PostgreSQL 数据库
- ✅ 构建并部署 API (NestJS)
- ✅ 构建并部署 Web (React)
- ✅ 配置环境变量
- ✅ 执行数据库迁移

⏱️ 首次部署约需 5-10 分钟

---

## 🔗 部署后的访问地址

部署完成后，你将获得：

| 服务 | 默认地址格式 |
|-----|-------------|
| API | `https://smart-dispatch-api.onrender.com` |
| Web | `https://smart-dispatch-web.onrender.com` |

### 登录信息
- **用户名**: `admin`
- **密码**: `admin123`

---

## 💰 费用 (全部免费)

| 服务 | Render 免费额度 | 是否足够 |
|-----|----------------|---------|
| Web Service (API) | 750 小时/月 | ✅ 足够 |
| PostgreSQL | 1GB 存储 | ✅ 足够 |
| Static Site (Web) | 无限 | ✅ 完全免费 |

> 💡 **休眠提示**: API 服务 15 分钟无访问会休眠，下次访问 30 秒自动唤醒

---

## 🔄 后续维护

### 代码更新
推送代码到 `main` 分支，Render **自动重新部署**

```bash
git add .
git commit -m "更新功能"
git push origin main
```

### 数据库变更
修改 `apps/api/prisma/schema.prisma` 后：
```bash
cd apps/api
npx prisma migrate dev --name 变更描述
git add prisma/migrations
git commit -m "数据库变更"
git push origin main
```

Render 会自动执行 `prisma migrate deploy`

---

## 🆘 常见问题

### Q: 部署失败怎么办？
A: 查看 Render Dashboard → 服务 → Logs 中的错误日志

### Q: 如何查看数据库？
A: Render Dashboard → 数据库 → Shell，可以执行 SQL 命令

### Q: 如何修改环境变量？
A: Render Dashboard → 服务 → Environment → 添加/修改变量 → 重新部署

### Q: 演示时 API 响应慢？
A: 这是休眠唤醒的正常现象，首次请求约 30 秒，后续请求正常

---

## 📞 需要帮助？

1. 查看 `RENDER_DEPLOYMENT.md` 详细文档
2. Render 官方文档: https://render.com/docs
3. 检查 Render Dashboard 的 Logs

---

## ✅ 部署检查清单

部署完成后，请验证：

- [ ] 访问 Web 地址能看到登录页面
- [ ] 使用 admin/admin123 能成功登录
- [ ] 能创建新的规则
- [ ] 能查看规则列表
- [ ] 能正常退出登录

---

**文档版本**: v1.0  
**创建时间**: 2026-02-17  
**祝部署顺利!** 🎉
